# Answer Normalizer Prompt

Normalize user answers into context field values.

Rules:

- Preserve custom answers.
- Store durable answers in Brand DNA.
- Store task-only answers in run context.
- Record source, confidence, timestamp, scope, and question ID.
- Flag ambiguous answers instead of flattening them into a wrong enum.

