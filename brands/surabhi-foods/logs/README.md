# Surabhi Foods provenance logs

Each external fetch, crawl, API request, and audit is recorded in the relevant category's `activity.jsonl`; immutable raw responses live under that category's `raw/` directory.
