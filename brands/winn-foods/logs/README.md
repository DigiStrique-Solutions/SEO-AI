# Winn Foods setup logs

External fetches, crawls, API calls, and audits are recorded by category in append-only `activity.jsonl` files, with immutable raw payloads under each category's `raw/` directory.
